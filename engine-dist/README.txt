Esta carpeta se usa al construir la versión instalable.

En desarrollo no se necesita ningún EXE aquí.

Para generar el motor empaquetado en Windows:
powershell -ExecutionPolicy Bypass -File .\scripts\build-engine-windows.ps1
